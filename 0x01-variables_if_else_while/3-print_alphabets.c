#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */

int main(void)
{
char uppercase = 'A'; // Declare the variable at the beginning of the block

while (uppercase <= 'Z') {
putchar(uppercase);
uppercase++
}
    
putchar('\n');

return (0);
}
